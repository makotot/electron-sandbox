{
  "name": "electron-sandbox",
  "version": "0.0.1",
  "description": "sandbox",
  "main": "main.js",
  "scripts": {
    "start": "gulp serve",
    "test": "gulp build",
    "package": "gulp build && cd app/ && npm install && ./node_modules/.bin/electron-packager ./ ElectronSandbox --platform=darwin --arch=x64 --version=0.35.4 --overwrite=true"
  },
  "author": "makotot",
  "license": "MIT",
  "dependencies": {
    "babel-core": "^6.3.15",
    "electron-packager": "^5.1.1",
    "electron-prebuilt": "^0.35.4",
    "flux": "^2.1.1",
    "node-static": "^0.7.7",
    "react": "^0.14.3",
    "react-dom": "^0.14.3",
    "react-fontawesome": "^0.3.3"
  },
  "devDependencies": {}
}